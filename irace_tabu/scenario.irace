# ============================================================================
# IRACE scenario — BAO Tabu Search, PROSTATE_tiny instances (8 angles x 24 dimlets)
# ============================================================================

targetRunner         = "/project/irace_tabu/target-runner"
trainInstancesFile   = "/project/irace_tabu/instances.txt"
parameterFile        = "/project/irace_tabu/parameters.irace"
logFile              = "/project/irace_tabu/results/irace.log"

# ── Presupuesto ───────────────────────────────────────────────────────────
maxTime              = 20000
targetRunnerTimeout  = 120

# ── Paralelismo ───────────────────────────────────────────────────────────
parallel             = 4

# ── Config inicial ────────────────────────────────────────────────────────
configurationsFile   = "/project/irace_tabu/init_config.txt"

# ── Semilla y output ──────────────────────────────────────────────────────
seed                 = 42
debugLevel           = 1

# ── Tests estadísticos ────────────────────────────────────────────────────
firstTest            = 2
testType             = "t-test"
